# create a hello world program in R

print("Hello, World!")

# make a small example plot

x <- c(1, 2, 3, 4, 5)
y <- c(2, 4, 6, 8, 10)

plot(x, y, main="A simple example plot", xlab="x", ylab="y", pch=19, col="blue")

